# Brief: #10 steps 1–2 — the skills tree, its test, and commune-setup

Lane `com10b_skills_opus`. Report to the Commune orchestrator (pane `w4:pE`). Worktree of commune-wiki on `feat/10-skills` from `main` (CLI prerequisites merged; 0.4.0 is being released as you start — assume it). `node_modules` symlink; dependency changes via `--virtual-store-dir` and **regenerate the lockfile; `pnpm install --frozen-lockfile --lockfile-only` must pass before each commit**. Do not push, do not open a PR. devon-wiki is read-only for you (step 2's PR on devon-wiki is a later lane; you write the template and the migration procedure here).

## Spec
`docs/plans/2026-09-03-authoring-skills-plan.md` — all of it; §2 D1–D10 are decided (see the orchestrator's rulings on `gh issue view 10 --comments`, including: content PRs merge on Devon's word; `aiGenerated` false when the summary sentence is his; refine corrections become rules only on "make that a rule"; dumps private by default with `visibility: public` per dump and a global setting in setup). Execute **§4 steps 1 and 2** (the tree + test; the setup skill and its template), not step 3 (run 2 is a separate lane on devon-wiki).

## Build
1. `skills/commune-dump/`, `skills/commune-write/`, `skills/commune-ship/`, `skills/commune-setup/` — each with `SKILL.md` ≤ 100 lines per D9 (frontmatter: `name`, `description` as a trigger ≤ 1,024 chars with the key use case first, `metadata`; `commune-setup` adds `disable-model-invocation: true` and `agents/openai.yaml` with `policy.allow_implicit_invocation: false`), `references/` for branch material (`refine.md`, `grill.md`, `handoffs.md` with the four file shapes and their frontmatter), `scripts/` only for things a skill must run deterministically (the side-by-side `review.html` builder over `commune render`; the `--version`/`schema: 1` preflight), `assets/WRITING.md` template in the fixed shape (Sentences / Titles / Notes / Frontmatter / Avoid / Verdicts). Skills call `node_modules/.bin/commune` from the wiki root; never install; stop with the one-line fix below 0.4.0. Write the skills as **rules an agent follows**, not descriptions (research #16): imperative, ordered, each step naming its input file and output file; every handoff a markdown file with YAML frontmatter in the wiki's `dumps/`.
2. `tests/skills.test.mjs` in `pnpm test` and CI: every `SKILL.md` parses, ≤ 100 lines, description ≤ 1,024 chars and starts with the trigger; the four folders exist; `scripts/` files are executable and run `--help`; `npx skills add <this checkout> --skill '*' -a claude-code -y` into a temp `HOME` installs four folders including `scripts/` and `assets/` (skip with a clear message if `npx skills` cannot run offline — say so). Add the distribution ADR the plan names (`docs/decisions/` or wherever the repo keeps them; if none, `docs/adr/0001-skills-distribution.md`).
3. README: an "Author with the skills" section (install command, the loop in one paragraph, the four names) replacing the "none of that loop is in this package" sentence; keep it honest that run 2 has not happened yet.
4. `commune-setup`: the interview is the onboarding pattern (explore first — read existing rules docs, content schemas, three live notes — then ask only the residue, recommended answer first, write answers into `WRITING.md`); the migration procedure for an existing rules doc is written as steps in `references/migrate.md` (devon-wiki's `docs/NOTE-WRITING-BIBLE.md` is the worked example: which sections map where); the `dumps` policy question (`never | opt-in | all`) and the public-repo gitignore question live here.

## Verify
`pnpm test` green with the new suite; `pnpm build` green; `git diff --exit-code public/backlinks.json`; frozen lockfile OK; `pnpm pack --json --out <tmp>` — `skills/` must NOT be in the npm tarball (`files` unchanged) since skills install via `npx skills`, not npm; a dry read-through: an agent given only `skills/commune-dump/SKILL.md` and a dump file can name every command it will run and every file it will write.

## Commits
`feat(#10): …` per step. Reply with head SHA, the tree (`find skills -type f`), test count, and the three places you had to decide something the plan left open (say what you chose and why).
