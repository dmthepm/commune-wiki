---
title: Nested Note
visibility: public
status: seed
---

A note one directory down. Astro slugifies each path segment separately, so
this one has to land at `/notes/sub-dir/nested-note/` — every segment
lowercased, not just the first.

It links to nothing and nothing links to it, so it is an isolated entry and the
only counts it moves are the totals.
